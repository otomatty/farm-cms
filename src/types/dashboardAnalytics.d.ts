export type UserData = {
	date?: string;
	month?: string;
	users: number;
};

export type SessionData = {
	date?: string;
	month?: string;
	sessions: number;
};

export type PageViewData = {
	date?: string;
	name: string;
	value: number;
};
